@model IEnumerable<E_Commerce.Models.Shipper>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container pb-5">
    <h2 style="text-align:center;">Shippers</h2>

    <p>
        @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
    </p>
    <table id="dtBasicExample" class="table table-striped table-bordered table-sm" cellspacing="0">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.shipperName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShipperDetail)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.shipperName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ShipperDetail)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.shipper_id }, new { @class = "btn btn-success" })

                    @Html.ActionLink("Delete", "Delete", new { id = item.shipper_id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }

    </table>
    </div>
